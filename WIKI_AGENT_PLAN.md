# LLM Wiki：工具驱动的构建与探索计划

状态：工具驱动基线已实施；新增一层跨文档摘要及单 QA Agent 渐进式披露。默认不创建检索或核验子 Agent。摘要质量、搜索算法与预算仍需真实模型实验确认。实现与验证记录见 `docs/wiki-agent-implementation.md`、`docs/cross-document-summaries.md`。

## Why

现有构建流程主要把目录索引和旧页面塞入提示词，让模型生成完整页面；问答流程则先检索，再将读过的 Markdown 一次性提交给回答模型。这会带来上下文截断、旧事实被覆盖、检索与回答脱节的问题。

希望让 LLM 主动调用工具整理已有知识、引入新信息，并围绕明确目的逐步探索和核验。全局结构先可见，具体内容按需读取，原文作为最终核验依据。

## Purpose

建立一套同时服务于 Wiki 构建和问答的工具调用机制：模型负责理解、选择与推理；工具负责查找、精确读取、来源定位和受控写入。

## Goal

- 不依赖少量样本预设主题分类；知识结构随实际内容生长。
- 构建与问答 Agent 均能查看整个 Wiki 的实时结构树，再选择目录和知识页。
- 保留事实、实体关系、限定条件及可验证的原文引用。
- Answer Agent 能持续请求新证据，证据充分时回答，未充分时继续探索或说明缺口。
- 局部任务只修改局部事实，不因未读取全部旧内容而覆盖其他知识。

## 核心流程

### 构建 Wiki

1. 工具保存新文档及来源版本，提供可定位的原文段落。
2. 构建 Agent 查看完整结构树，理解当前知识分布。
3. 提取新文档中的实体、事实和关系；事实保留时间、条件、否定及不确定性。
4. 调用目录浏览、搜索和读取工具，寻找已有实体与相关知识；必要时回原文核实。
5. 决定补充已有事实、添加证据、记录冲突，或创建新知识页。
6. 使用受控写入工具提交事实增量和引用；不以局部上下文重写整个旧页。
7. 对有证据且有独立价值的关系建立关系页；批次结束后，从带原文引用的事实生成一层跨文档摘要。摘要记录依赖页面版本及逐条原文出处，过期时退出正常检索，重建后恢复。

### 探索与回答

1. Answer Agent 首先看到整个 Wiki 的结构树，包含目录及简短说明；不是预先筛选的少量摘要。
2. 根据问题选择一个或多个可能相关的目录，调用工具浏览和搜索知识页。
3. 对跨文档问题可先检索摘要层，依次展开概览、具体陈述、选定陈述的事实依据；细节问题可直接搜索知识页。两条入口均保留。
4. 需要核验时，通过来源引用读取原文段落及必要上下文。
5. 在同一个 QA Agent 上下文中完成局部查证；根据已发现的实体和证据缺口改写查询，不创建子 Agent。
6. 每轮判断证据是否充分；不足则换目录、改查询或沿关系继续查找。
7. 证据充分后由 premium Answer Agent 回答并引用来源；预算耗尽时说明缺口。

结构树展示当前实际知识组织，不等于采样生成的强制分类体系。结构浏览、内容搜索和原文读取均应可用，避免单一路径遗漏证据。

## Agent 任务契约

每项任务明确以下字段。当前 QA 在同一个 Agent 内执行这些任务；以下子任务契约也保留给历史兼容接口：

- **Why**：为何需要此次操作或核验，来自上层的哪个疑问。
- **Purpose**：本次任务在整体流程中的作用。
- **Goal**：需要交付的具体结果与完成条件。
- **Input / Scope**：实体身份、待核验陈述、允许查找的范围和必要背景。
- **Tools / Budget**：可用工具、写入权限及调用预算。
- **Return**：发现、原文片段、来源版本与位置、冲突及未解决事项。

子 Agent 应核验陈述是否成立，而非仅寻找支持上层猜测的材料。结果区分已找到、当前范围未找到、证据冲突和预算耗尽。

## 可分配的实现任务

| 任务 | Why | Purpose | Goal / 验收条件 | 主要代码范围 |
|---|---|---|---|---|
| A. 来源与事实基础 | 摘要、文件名和事件日期不能可靠标识证据版本 | 建立可核验的数据基础 | 来源 ID、版本 ID、原文位置稳定；事实保留限定条件；事件时间与来源版本分开 | `preprocess_bench.py`、`bench_ingest.py` 的来源处理；新增事实存储模块 |
| B. 结构树与读取工具 | Agent 需要先了解全局，再自主选择读取范围 | 建立统一探索入口 | 实时展示实际结构，包含未提交文件；支持目录、页面、章节和原文段落读取；不依赖手工 Markdown 索引 | `wiki_retriever.py`、页面元数据约定 |
| C. 工具驱动构建 | 两次提示词生成难以支持主动查找和安全更新 | 让构建 Agent 自主整理新旧知识 | 循环查找与消歧；受控增量写入；未读完整旧页也不会覆盖其他事实；链接写入前验证 | `bench_ingest.py`、`bench_config.py`、`configs/` |
| D. 搜索替代方案 | 当前子串匹配和手工加分会抬高无关页面排名 | 提供可靠候选，支持 Agent 自主选择 | 移除现有子串加分；比较标准 BM25、完整实体查找及可选重排；来源去重；比较候选数 5/10/15 | `wiki_retriever.py` 搜索部分 |
| E. 回答探索闭环 | 回答者需要根据新证据持续查证 | 由回答需求驱动下一轮查证 | 单个 QA Agent；结构树起步；摘要逐步展开；记录证据缺口和新增证据；避免重复探索；premium 模型最终回答 | `wiki_agent.py`、`run_qa.py` |
| F. 验证与存量修复 | 最终答对不代表来源、链接和事实保存正确 | 验证架构并修复已有数据 | 修复格式解析和链接；检查事实保存、来源引用、检索证据覆盖、答案质量和调用成本 | `evaluate.py`、数据校验工具 |

代码范围均相对于 `llm_wiki_bench/`，`configs/` 除外。先约定 A/B 的接口，再实施 C/D/E，最后做 F 的端到端验证；验证标准应在实现前确定。多名 Agent 协作时明确到函数或新模块的所有权，避免同时修改同一区域，不覆盖其他人的改动。

## 必须同步处理的严重问题

- **数据丢失**：长文截断、旧页未完整读取却整页覆盖；应保证全文可访问，修改范围受控。
- **来源错误**：文件名模糊匹配、同名原文不保存新版本；应改用稳定身份和版本引用。
- **格式解析**：代码围栏包住整页导致 aliases / tags 无法解析；需修复存量文件并重建检索数据。
- **实体混淆**：同名文件不应直接合并为同一实体。
- **静默失败**：部分写入失败后整批仍被缓存为成功；应逐文档验证产物并支持重试。
- **回答偏移**：移除武断的属性判断规则，明确证据、推断和未知的边界。

## 未定项与设计边界

- 搜索算法尚未最终确定；BM25 是候选基线，不预设 embedding 必然更好或更差。
- 已决定使用单个 QA Agent，借鉴 Ψ-RAG 的检索—判断—改写查询—继续检索/回答循环。
- 先实现一层跨文档总结。总结是可追溯的派生证据，可引用其路径、版本与陈述 ID，但必须同时核验并引用支撑这些陈述的原文；不能把生成的总结当成独立原始证据。
- 当前用显式链接和 BM25 提议跨页组合，由模型判断是否值得总结；不是完整复现 Ψ-RAG 的向量摘要树算法，不递归总结已有总结。
- 增量更新减少维护范围，但不意味着不需要更新冲突事实、关系和总结。
- 渐进探索不能恢复从未保存的内容；“未找到”也不等于“信息不存在”。
